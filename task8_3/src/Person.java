public abstract class Person {
    String firstName, lastName;
    int birthYear;
    int currentDate;
    int age;

    //for some reason I had to delete the constructor that was here, and the code now works

    public int getAge(){
        return age;
    }
}
